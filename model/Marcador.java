package prototipo.model;

import javafx.scene.shape.Rectangle;
import javafx.scene.shape.Rectangle;
import javafx.scene.paint.Color;
public class Marcador extends Rectangle{
	    /*
		Guarda as dimensoes do marcado
	*/  
	
	public Marcador(){
		setWidth(0);
		setHeight(0);
		setX(0);
		setY(0);
		setStroke(Color.RED);
		setFill(Color.TRANSPARENT);
    }
	
}